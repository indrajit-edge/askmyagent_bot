# ==============================================================================
# AskMyAgent — Backend Environment Configuration Template
# (Internal API for the VM Telegram bot; no Telegram listening here)
# ==============================================================================

# Server & Network
PORT=4000
NODE_ENV=production
FRONTEND_URL=https://askmyagent.com
CORS_ORIGIN=https://askmyagent.com
DATABASE_URL=
DB_SSL=true
DATABASE_PATH=backend/database.sqlite

# Admin Control Center Authentication
ADMIN_USERNAME=
# Prefer ADMIN_PASSWORD_HASH generated with bcrypt. Use ADMIN_PASSWORD only for local development.
ADMIN_PASSWORD=
ADMIN_PASSWORD_HASH=

# Comma-separated public IP addresses allowed to access the Admin
# Control Center and administrative APIs.
#
# Production MUST be configured.
# If missing in production, admin access fails closed.
#
# Never expose this through VITE_* variables.
ADMIN_ALLOWED_IPS=203.0.113.1,198.51.100.2

# Cryptography & Security Secrets
# Generate with: node -e "console.log(crypto.randomBytes(32).toString('hex'))"
ENCRYPTION_KEY=
# Generate with: node -e "console.log(crypto.randomBytes(32).toString('base64url'))"
JWT_SECRET=

# Internal API shared secret for the VM bot -> backend /api/internal/* calls.
# The VM bot sends it in the x-internal-token header. Must match the VM's value.
# Generate with: openssl rand -hex 32
INTERNAL_API_TOKEN=

# NOTE: TELEGRAM_BOT_TOKEN / TELEGRAM_WEBHOOK_URL / TELEGRAM_WEBHOOK_SECRET were removed.
# The backend never talks to the Telegram Bot API; the VM Python bot owns the token.

# Google Cloud OAuth 2.0 Credentials (Web Application)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=https://api.askmyagent.com/api/oauth/callback

# Optional
GEMINI_API_KEY=
COOKIE_DOMAIN=
